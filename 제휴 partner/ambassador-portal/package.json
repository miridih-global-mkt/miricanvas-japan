{
  "name": "mcjp-ambassador-portal",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "db:init:local": "wrangler d1 execute ambassador_portal --local --file=schema.sql",
    "db:seed:local": "wrangler d1 execute ambassador_portal --local --file=seed.sql",
    "db:init:remote": "wrangler d1 execute ambassador_portal --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
